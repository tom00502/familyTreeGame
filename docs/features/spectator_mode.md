# 旁觀者模式 (Spectator Mode)

## 📌 模式概述

**目標**：讓未參與遊戲的使用者以「只讀」身份觀看遊戲進行中的狀況

**玩家體驗**：旁觀者可即時閱覽所有玩家的答題動態與族譜建構歷程，但無法提交任何答案或影響遊戲結果

**觸發條件**：

- 使用者於遊戲「進行中 (In-Game)」或「結算中 (Settling)」時加入房間
- 詳見 [room_management.md](../management/room_management.md#階段-0房間建立與加入) 中的加入流程

---

## 🚪 旁觀者進入流程

### 自動偵測與導向

使用者開啟 `/room/:id` 時，系統在連線前先呼叫 `GET /api/room/:id/status`：

```
flowchart:
  訪問 /room/:id
    ↓ HTTP GET /api/room/:id/status
  房間不存在           → 顯示「房間不存在」錯誤
  isLocked = false     → 正常顯示身份輸入表單（等待中）
  isLocked = true      → 自動導向 /room/:id/dashboard
```

**重要**：旁觀者進入 `/room/:id/dashboard` **不需填寫姓名、性別、出生日期**，系統以匿名方式建立旁觀者連線。

### 旁觀者無身份加入流程（`/room/:id/dashboard`）

1. 頁面載入後直接建立 WebSocket 連線
2. 連線成功後傳送 `spectator:watch` 事件（僅帶 `roomId`，無個人資料）
3. 伺服器驗證房間是否仍在進行中：
   - ✅ 進行中 → 回傳 `spectator:joined` + `spectator:sync`（完整即時狀態）
   - ❌ 遊戲未開始或不存在 → 回傳 `spectator:redirect`，前端導回 `/room/:id`
4. 收到 `spectator:sync` 後立即顯示旁觀者看板

### API 規格

**`GET /api/room/:id/status`**

```json
Response 200:
{
  "roomId": "ABCD1234",
  "roomName": "王家族譜",
  "status": "relationship-scan",
  "isLocked": true,
  "playerCount": 4
}

Response 404:
{ "statusCode": 404, "message": "房間不存在" }
```

### WebSocket 事件：`spectator:watch`

```json
Client → Server:
{ "type": "spectator:watch", "roomId": "ABCD1234" }

Server → Client（成功）:
{ "type": "spectator:joined", "spectatorId": "..." }
{ "type": "spectator:sync", "roomName": "...", "roomStatus": "...", "players": [...], "answerHistory": [...], "mvft": { ... } }

Server → Client（遊戲尚未開始）:
{ "type": "spectator:redirect", "target": "join" }
```

---

## 🖥️ 旁觀者看板 (Spectator Dashboard)

旁觀者看板由兩個主要區塊組成，並排呈現於同一頁面：

```
┌─────────────────────────────────────────────────────────────┐
│                     旁觀者看板                               │
├──────────────────────┬──────────────────────────────────────┤
│   📋 答題監測區塊    │         🌳 即時族譜區塊              │
│   (左側 / 上方)      │         (右側 / 下方)                │
└──────────────────────┴──────────────────────────────────────┘
```

- **桌機版 (≥768px)**：左右並排，答題監測佔 40%，族譜佔 60%
- **行動版 (<768px)**：上下堆疊，答題監測在上，族譜在下

---

## 📋 答題監測區塊 (Answer Monitor Panel)

### 功能說明

即時呈現所有答題玩家的狀態，讓旁觀者清楚掌握遊戲進行節奏。

### 子區塊結構

#### 1. 玩家狀態列表 (Player Status List)

列出房間內所有**參與遊戲的玩家**（不含旁觀者），每位玩家一列：

| 欄位         | 說明                                                  |
| ------------ | ----------------------------------------------------- |
| 玩家名稱     | 顯示該玩家填寫的姓名                                  |
| 連線狀態     | 🟢 在線 / 🟡 重連中 / 🔴 離線                         |
| 當前作答題目 | 系統目前派給該玩家的題目摘要（題目方向 + 被詢問對象） |
| 答題進度     | 該玩家本局已答題數 / 總派題數（例如：`5 / 8`）        |

**更新頻率**：玩家每次提交答案後立即更新

**隱私保護**：

- 只顯示題目摘要（例如：「確認與小明的關係」），不顯示題目完整內容
- 不顯示玩家尚未提交的輸入內容

#### 2. 答題歷史紀錄 (Answer History Log)

以時間軸方式列出本局所有已確認的答案，最新紀錄顯示於頂部：

```
[ 14:23:05 ]  王小明  回答：「王大華 是他的 爸爸」           ✅ 確認
[ 14:23:01 ]  李美玲  回答：「陳阿嬤 是她的 奶奶」           ✅ 確認
[ 14:22:58 ]  王小明  回答：「李美玲 屬於 媽媽那邊的人」     ✅ 確認
[ 14:22:50 ]  陳大勇  回答：「不知道 王阿公 的姓名」         ⏭️ 跳過
```

**欄位說明**：

| 欄位     | 說明                                    |
| -------- | --------------------------------------- |
| 時間戳記 | 答案提交的時間（`HH:MM:SS` 格式）       |
| 玩家名稱 | 提交答案的玩家                          |
| 答案摘要 | 答題的簡短描述（不暴露族譜敏感資訊）    |
| 狀態標記 | ✅ 確認 / ⏭️ 跳過 / ⚠️ 待驗證 / ❌ 衝突 |

**衝突標記說明**：

- 若同一節點的資料遭到其他玩家質疑，該筆紀錄標記為 `❌ 衝突`
- 衝突詳情不在此展示，需等待第三階段大揭曉解決

**最大顯示筆數**：顯示最近 50 筆，超出後可向下滾動查看完整歷史

---

## 🌳 即時族譜區塊 (Live Family Tree Panel)

### 功能說明

以視覺化方式即時呈現族譜的建構狀況。與遊戲玩家不同，旁觀者可以**全程看見族譜的增長過程**，是旁觀者模式的核心趣味體驗。
參考design/family_tree.md繪畫原則

### 顯示內容

#### 節點顯示規則

| 節點類型             | 顯示方式                                      |
| -------------------- | --------------------------------------------- |
| 玩家節點（資料完整） | 實線圓框 + 姓名 + 性別圖示                    |
| 虛擬節點（已命名）   | 實線圓框 + 姓名 + 性別圖示（半透明效果）      |
| 虛擬節點（未命名）   | 虛線圓框 + 「？」或「未知長輩」               |
| 剛更新的節點         | 閃爍高亮動畫（持續 2 秒，使用輔色 `#D4AF37`） |

#### 關係線顯示規則

| 關係類型           | 線條樣式 |
| ------------------ | -------- |
| 親子關係（已確認） | 實線     |
| 親子關係（推導中） | 虛線     |
| 配偶關係           | 雙線     |
| 待確認關係         | 灰色虛線 |

### 即時更新機制

當玩家提交答案且系統確認可以更新族譜時，旁觀者看板立刻呈現：

1. **新增節點**：新節點以縮放動畫從小到大出現
2. **節點資訊填入**：節點閃爍高亮 2 秒後穩定顯示新資訊（例如：姓名由「？」變為「王阿公」）
3. **新增關係線**：關係線以繪製動畫從父節點拉向子節點
4. **節點合併**：若兩個虛擬節點確認為同一人，執行合併動畫

**觸發條件**（下列任一情況皆觸發更新）：

- 玩家完成第一階段的關係確認，產生新的虛擬節點或連結關係
- 玩家完成第二階段的節點命名或屬性填充
- 玩家確認虛擬節點為某一已知成員（節點匯聚）

### 族譜完整度進度條

在即時族譜區塊頂部顯示進度條：

```
族譜完整度  ████████░░░░░░░  52%   (關鍵節點 13/25 已確認)
```

- **計算方式**：已確認關鍵路徑節點數 / MVFT 所需關鍵節點總數
- **關鍵節點定義**：詳見 [mvft.md](../concepts/mvft.md) 中的關鍵路徑定義
- **顏色編碼**：
  - 0%–39%：`#8B8278`（灰棕，起步）
  - 40%–69%：`#D4AF37`（金色，進行中）
  - 70%–100%：`#8B2635`（深紅，即將完成）

---

## 🔒 旁觀者權限與限制

### 允許的操作

| 操作                 | 說明                                   |
| -------------------- | -------------------------------------- |
| 查看答題監測區塊     | 完整可見                               |
| 查看即時族譜         | 完整可見（與玩家不同，玩家看不到族譜） |
| 滾動/縮放族譜        | 支援滑鼠滾輪縮放、拖曳平移             |
| 查看答題歷史         | 完整可見，可向下滾動                   |
| 遊戲結束後查看大揭曉 | 同步觀看大揭曉動畫                     |

### 禁止的操作

| 操作         | 原因                 |
| ------------ | -------------------- |
| 提交任何答案 | 不影響遊戲結果       |
| 觸發關係確認 | 保護遊戲公平性       |
| 接受題目派發 | 系統不為旁觀者派題   |
| 計入分數排名 | 旁觀者不列入最終排名 |

---

## 🔄 狀態同步規則

旁觀者看板透過 WebSocket 接收以下事件（只讀，不發送）：

| 事件類型                     | 觸發時機           | 更新目標                    |
| ---------------------------- | ------------------ | --------------------------- |
| `spectator:joined`           | 連線確認成功       | 設定旁觀者模式              |
| `spectator:sync`             | 首次連線時完整同步 | 初始化看板所有資料          |
| `spectator:redirect`         | 遊戲尚未開始       | 導回 `/room/:id`            |
| `spectator:answer_submitted` | 玩家提交答案       | 答題歷史紀錄 + 玩家答題進度 |
| `spectator:player_status`    | 玩家狀態改變       | 玩家狀態列表                |
| `spectator:tree_updated`     | 族譜結構更新       | 即時族譜 SVG                |
| `game_ended`                 | 遊戲時間結束       | 切換至大揭曉畫面            |

**延遲標準**：旁觀者看板更新延遲應 ≤ 500ms（與一般玩家狀態同步標準相同）

---

## 🎨 視覺設計規範

旁觀者看板遵循全局視覺規範，詳見 [visual_system.md](../design/visual_system.md)。以下為旁觀者看板專屬補充：

### 旁觀者標識

- 頁面頂端顯示「👁️ 旁觀者模式」Badge，背景色 `#8B8278`，文字白色
- 所有互動按鈕（答題相關）以 `disabled` 狀態呈現，懸停時顯示 tooltip：「旁觀者無法答題」

### 空狀態顯示

| 情境         | 顯示文字                            |
| ------------ | ----------------------------------- |
| 尚無答題紀錄 | 「遊戲即將開始，等待玩家答題中...」 |
| 族譜節點為零 | 「族譜建構尚未開始」                |
| 玩家全部離線 | 「所有玩家暫時離線，等待重連...」   |

---

## 📌 邊界情況處理

| 情境                                                   | 處理方式                                                    |
| ------------------------------------------------------ | ----------------------------------------------------------- |
| 旁觀者直接輸入 `/room/:id/dashboard` URL（遊戲未開始） | 伺服器回傳 `spectator:redirect`，前端導回 `/room/:id`       |
| 旁觀者在遊戲結束後加入                                 | 直接進入大揭曉畫面，無法查看遊戲過程回放                    |
| WebSocket 斷線                                         | 顯示「連線中斷，嘗試重連...」Banner，重連後自動同步最新狀態 |
| 族譜節點過多（> 30 節點）                              | 啟用縮放與平移功能，預設縮略圖比例以容納全部節點            |
| 多個旁觀者同時連線                                     | 各自獨立 WebSocket 連線，互不影響                           |

---

**相關文件**：

- [room_management.md](../management/room_management.md) - 旁觀者加入流程、WebSocket 連線管理
- [mvft.md](../concepts/mvft.md) - 關鍵路徑與節點定義
- [visual_system.md](../design/visual_system.md) - 視覺規範
- [animation_specs.md](../design/animation_specs.md) - 動畫效果規範
